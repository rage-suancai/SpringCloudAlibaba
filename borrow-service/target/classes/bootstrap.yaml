spring:
  application:
    name: borrow-service
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:8858
      web-context-unify: false
#      block-page: /blocked

